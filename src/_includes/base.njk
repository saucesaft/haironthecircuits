<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="It’s (j)ust (e)leventy and (t)ailwind … OK, and a few other things; it’s still really small though.">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="/css/styles.css">
  </head>
  <body class="bg-picnic bg-cover">
    <main class="flex flex-col w-full h-screen">

      {% include "topbar.njk" %}

      <div class="m-2 md:mt-0 md:m-4 grow bg-zinc-800 flex items-center justify-center">

	<article class="prose md:prose-lg lg:prose-xl prose-invert p-4">
	  {{ content | safe }}
	</article>

      </div>


      <nav class="flex items-center justify-between flex-wrap m-2 md:mt-0 md:m-4 space-y-1 md:space-y-2 lg:space-y-0">

	  {% for banner in banners.list -%}
	    
	  {% if banner.url != "" %}
	      <a href="{{ banner.url }}">
	  {% else %}
	      <a>
	  {% endif %}

		  <img src="{{ banner.media }}" class="banners" />
	      </a>
	  {% endfor %}

      </nav>

    </main>
  </body>
</html>
